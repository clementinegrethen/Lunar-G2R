parameter texture fulltex;
global factor vec2(1);
global offset vec2(0);
global fullsize vec2(0);

vec4 lookup2D(texture tex, vec2 uv, scalar lod)
{
    vec2 _uv = uv * factor + offset;
    return lookupLOD(fulltex, _uv, lod);
}

vec2 textureSize()
{
    return fullsize;
}
